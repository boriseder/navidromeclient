//
//  AlbumBackgroundView.swift
//  NavidromeClient
//
//  Created by Boris Eder on 04.09.25.
//

/*
 

import SwiftUI

// MARK: - Background
struct AlbumBackgroundView: View {
    @EnvironmentObject var navidromeVM: NavidromeViewModel
    let cover: UIImage?
    
    var body: some View {
        Group {
            if let cover = cover {
                Image(uiImage: cover)
                    .resizable()
                    .aspectRatio(contentMode: .fill)
                    .blur(radius: 50)
                    .opacity(0.5)
                    .ignoresSafeArea()
            } else {
                LinearGradient(
                    colors: [Color(.systemBackground), Color(.systemGray6)],
                    startPoint: .top,
                    endPoint: .bottom
                )
                .ignoresSafeArea()
            }
        }
    }
}

*/
